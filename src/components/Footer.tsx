import { Grid } from '@mui/material';
import React from 'react';

export const Footer = () => {
  const style = {
    padding: '20px 0',
  };
  return (
    <Grid
      container
      display="flex"
      justifyContent="center"
      alignItems="center"
      style={style}
    >
      Footer
    </Grid>
  );
};
